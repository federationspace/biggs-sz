apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: romm-user-password
  namespace: cnpg-system
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-connect
  target:
    creationPolicy: Owner
  data:
  - secretKey: username
    remoteRef:
      key: "romm-user-password"
      property: username
  - secretKey: password
    remoteRef:
      key: "romm-user-password"
      property: password
