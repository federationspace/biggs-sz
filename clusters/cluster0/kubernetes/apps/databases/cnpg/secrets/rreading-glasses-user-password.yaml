apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: rreading-glasses-user-password
  namespace: cnpg-system
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-connect
  target:
    creationPolicy: Owner
  data:
  - secretKey: password
    remoteRef:
      key: "rreading-glasses-user-password"
      property: password
  - secretKey: username
    remoteRef:
      key: "rreading-glasses-user-password"
      property: username
