apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: pinepods-user-password
  namespace: cnpg-system
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-connect
  target:
    creationPolicy: Owner
  data:
  - secretKey: username
    remoteRef:
      key: "pinepods-user-password"
      property: username
  - secretKey: password
    remoteRef:
      key: "pinepods-user-password"
      property: password
