apiVersion: v1
kind: ConfigMap
metadata:
  name: epub-only-config
  namespace: media
  labels:
    app.kubernetes.io/name: epub-only
    app.kubernetes.io/component: config
data:
  SCAN_ROOT: "/ebooks/"
  SERVER_PORT: "8080"
  DRY_RUN: "false"
  LOG_LEVEL: "info"
