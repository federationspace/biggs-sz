---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: epub-only
  namespace: epub-only
spec:
  hostnames:
    - epub-only.gregbob.net
  parentRefs:
    - name: wildcard-gregbob-net
      namespace: networking
  rules:
    - backendRefs:
        - name: epub-only
          port: 80
      matches:
        - path:
            type: PathPrefix
            value: /
