apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pinepods-downloads
  namespace: media
  labels:
    app: pinepods
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
  storageClassName: ceph-block
