apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: romm-assets
  namespace: media
  labels:
    app: romm
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: local-path
