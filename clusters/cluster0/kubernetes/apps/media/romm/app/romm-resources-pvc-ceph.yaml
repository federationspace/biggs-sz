apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: romm-resources-ceph
  namespace: media
  labels:
    app: romm
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
  storageClassName: ceph-block
